import React from 'react';

const UserInfo = (props) => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>내 정보</h1>
    </div>
  );
};

export default UserInfo;