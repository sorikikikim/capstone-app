import React from 'react';

const QnA = (props) => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>문의하기</h1>
    </div>
  );
};

export default QnA;
